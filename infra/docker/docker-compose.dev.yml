services:
  postgres:
    env_file:
      - ../../.env
    image: postgres:16
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../backend/auth-service/src/main/resources/db/migration/V1__init.sql:/docker-entrypoint-initdb.d/001_init.sql:ro
    mem_limit: 512m
    cpus: 1.0
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 30

  redis:
    env_file:
      - ../../.env
    image: redis:7
    ports:
      - "6379:6379"
    mem_limit: 128m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 30

  auth-service:
    env_file:
      - ../../.env
    build:
      context: ../../backend
      dockerfile: auth-service/Dockerfile
    ports:
      - "8081:8081"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0
    environment:
      SERVER_PORT: "8081"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false

  user-service:
    env_file:
      - ../../.env
    build:
      context: ../../backend
      dockerfile: user-service/Dockerfile
    ports:
      - "8084:8084"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0
    environment:
      USER_SERVER_PORT: "8084"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false

  project-service:
    env_file:
      - ../../.env
    environment:
      PROJECT_SERVER_PORT: "8082"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false
    build:
      context: ../../backend
      dockerfile: project-service/Dockerfile
    ports:
      - "8082:8082"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0

  halloffame-service:
    env_file:
      - ../../.env
    environment:
      HALL_OF_FAME_SERVER_PORT: "8085"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false
    build:
      context: ../../backend
      dockerfile: halloffame-service/Dockerfile
    ports:
      - "8085:8085"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0

  forum-service:
    env_file:
      - ../../.env
    environment:
      FORUM_SERVER_PORT: "8083"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false
    build:
      context: ../../backend
      dockerfile: forum-service/Dockerfile
    ports:
      - "8083:8083"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0

  ai-service:
    env_file:
      - ../../.env
    environment:
      AI_SERVER_PORT: "8086"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false
    build:
      context: ../../backend
      dockerfile: ai-service/Dockerfile
    ports:
      - "8086:8086"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0

  admin-service:
    env_file:
      - ../../.env
    environment:
      ADMIN_SERVER_PORT: "8088"
      JAVA_TOOL_OPTIONS: >-
        -Xms128m -Xmx512m
        -XX:MaxMetaspaceSize=192m
        -XX:+UseG1GC
        -XX:+UseStringDeduplication
        -Dspring.main.lazy-initialization=true
        -Dspring.jmx.enabled=false
    build:
      context: ../../backend
      dockerfile: admin-service/Dockerfile
    ports:
      - "8088:8088"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 750m
    cpus: 1.0

  chat-service:
    env_file:
      - ../../.env
    build:
      context: ../../backend
      dockerfile: chat-service/Dockerfile
    ports:
      - "8090:8090"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 256m
    cpus: 0.5

volumes:
  postgres_data:
